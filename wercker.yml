box: golang:latest
build:
  steps:
    - arjen/hugo-build:
        version: "0.26"
deploy:
  steps:
    - duleorlovic/ftp-deploy:
      destination: $FTP_DESTINATION
      username: $FTP_USERNAME
      password: $FTP_PASSWORD
      subfolder: public
